<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:orientation="vertical"
    tools:context=".selectingfromtodestinaiton">

    <!-- SearchView for entering current location -->
    <SearchView
        android:id="@+id/search_view_current_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:iconifiedByDefault="false"
        android:queryHint="Enter current location" />

    <!-- SearchView for entering destination -->
    <SearchView
        android:layout_marginTop="10dp"
        android:id="@+id/search_view_destination"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:iconifiedByDefault="false"
        android:queryHint="Enter destination" />

    <!-- Button to set location on map -->
    <Button
        android:layout_marginTop="15dp"
        android:drawableLeft="@drawable/map"
        android:textColor="@color/black"
        android:backgroundTint="@color/white"
        android:id="@+id/button_set_location_on_map"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Set location on map" />

    <!-- Dynamic buttons layout for saved places -->
    <LinearLayout
        android:id="@+id/layout_saved_places"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <!-- Dynamically add buttons here -->
    </LinearLayout>
    <ScrollView
      android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
    <!-- Other buttons layout -->
    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <Button
            android:id="@+id/button_option1"
         android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Option 1" />

        <Button
            android:id="@+id/button_option2"
android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Option 2" />

        <!-- Add more buttons as needed -->

    </LinearLayout>
    </ScrollView>

</LinearLayout>